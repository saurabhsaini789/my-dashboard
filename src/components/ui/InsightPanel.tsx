import React from "react";
import { dashboardTokens } from "@/lib/design-system"; export type DashboardInsight = { id: string; title: string; summary: string; nextStep: string;
}; type InsightPanelProps = { insights: DashboardInsight[];
}; export function InsightPanel({ insights }: InsightPanelProps) { if (insights.length === 0) { return ( <div className="rounded-xl border border-border bg-card p-5 "> <h2 className={dashboardTokens.text.section}>Insight</h2> <p className={`mt-2 ${dashboardTokens.text.muted}`}> No patterns need attention. Keep the day simple. </p> </div> ); } return ( <div className="rounded-xl border border-border bg-card p-5 "> <div className="mb-4"> <h2 className={dashboardTokens.text.section}>Insight</h2> <p className={dashboardTokens.text.muted}>Clear next steps from today&apos;s signals.</p> </div> <div className="flex flex-col divide-y divide-zinc-200 dark:divide-zinc-800"> {insights.map((insight) => ( <div key={insight.id} className="py-4 first:pt-0 last:pb-0"> <h3 className="text-sm font-semibold text-zinc-950 ">{insight.title}</h3> <p className={`mt-1 ${dashboardTokens.text.muted}`}>{insight.summary}</p> <p className="mt-2 text-sm font-medium text-foreground"> Next: {insight.nextStep} </p> </div> ))} </div> </div> );
}
